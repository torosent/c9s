# Install

Until the v0.1.0 release ships, install from source:

```bash
git clone https://github.com/torosent/c9s.git
cd c9s
make build
./bin/c9s --version
```

Once Plan 8 ships, the recommended install will be:

```bash
brew tap torosent/c9s
brew install c9s
```
